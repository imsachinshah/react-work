import World from './World';

function App()  {
    return (
      // only one element we can return from this so for this we use fragmatics for returning multiple elements inside it 
      // <> </>
      <>
        <h1>Hello React!!!</h1>
        <World />
      </>
    );
}

export default App;
