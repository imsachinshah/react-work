// name of function should be in Capital else this will not render in webpage 
// also it will not show error in this app and show error when app created using VITE 

// this is component so when we use html in js file we should use .jsx extention for this type of file

function World(){
    return (
        <h2>Hello World from World components</h2>
    );
}

export default World;